{
  "name": "edge-utils",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.3",
    "msw": "^2.13.2",
    "vitest": "^4.1.4",
    "wrangler": "^4"
  }
}
